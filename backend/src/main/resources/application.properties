spring.application.name=Gapdle

# Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/gapdle
spring.datasource.username=gapdle_user
spring.datasource.password=jC11f1rst%U5ER
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Settings (Hibernate ORM)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none

# Show SQL queries in the console (for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
